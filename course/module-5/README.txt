before running the inference do:
    - need to use `qwak configure` and set the API Key from your qwak env
    - when deploying the model on qwak as realtime under advanced setting you need to set the timeout filed to a bigger
    value (eg: 50000ms)